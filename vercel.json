{
  "buildCommand": "pnpm --filter @workspace/salon-website build",
  "outputDirectory": "artifacts/salon-website/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "env": {
    "BASE_PATH": "/"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
